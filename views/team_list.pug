extends layout

block content
  h1(class = "text-center")= title

  div(class = "container")
    div(class = "row")
        each team in teamData 
            div(class = "col-4 text-center mt-1")
                a(href=team.url) #{team.team_title}